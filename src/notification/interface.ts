export enum NotificationStatus {
    IsSent = 'is-sent',
    IsRead = 'is-read',
    IsError = 'is-error'
}

export enum NotificationType {
    FILE = 'file',
    TEXT = 'text',
    MESSAGE = 'message'
}